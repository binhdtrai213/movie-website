export const getImage = (code: string) => {
  return `https://img.youtube.com/vi/${code}/maxresdefault.jpg`
}
export const getVideo = (code: string) => {
  return `https://www.youtube.com/embed/${code}`
}
